<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <gradient
        android:startColor="@color/some_grey"
        android:centerColor="@color/some_grey"
        android:endColor="@color/some_grey"
        android:angle="90"
        />
   <!-- <stroke android:width="1dp" android:color="@color/light_gray" />  -->
    <corners
        android:radius="2sp"/>
</shape>